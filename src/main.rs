mod bitboard;
use bitboard::*;
mod attacks;
use attacks::*;
mod magic;
use magic::*;

// use std::time::Instant;
// use std::io;
// use std::io::Write;
// use std::io::stdin;

fn main() {
    init_leapers_attacks();
}
